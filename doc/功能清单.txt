1. 基本功能
	看topic, broker机器与配置， consumer, 数据
    1.1 Broker 的 id与ip
    1.2 topic name, partitions, data
    1.3 consumer
2. 监控Lag发告警
3. 导出指定时间的数据
4. 健康诊断


1. 增加replication factor， 分区数
2. 增量备份，导入导出
7. 增强测试功能
    清指定topic的数据，数据重入功能
    consumer offset 回退




查
zookeeper 里的数据
kafka 里的数据

改
topic副本数， 分区数，新加数据，重入
大批量导入导出
consumer offset 按时间回退

打包
两个版本
kafka-box
kafka-box-readonly
